from turbo import logger

# Logger objects for internal turbo use
model_log = logger.getLogger('turbo.model')
util_log = logger.getLogger('turbo.util')
app_log = logger.getLogger('turbo.app')
helper_log = logger.getLogger('turbo.helper')